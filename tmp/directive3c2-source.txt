Directive 3c.2: Gate Repair + Substitution-Structured CannibalizationPart 1: Pearson gate repair (statistical, no simulation changes)Per-seed paired-delta floor widens from −0.03 to −0.06 (~2σ of the paired-delta noise at N≈900, r≈0.5; derivation above goes in the audit record). Mean-delta gate stays binding at ≥ −0.02.Under the repaired gate, 3c's freshness checkpoint retroactively passes (deltas −0.033/−0.017/+0.025, mean −0.008). Freshness mechanics, counters, and telemetry are frozen as delivered. Record the determination.For 3d, the Pearson guard will use ≥6 seeds with the same mean/floor structure — note this now so the 3d directive inherits it.Part 2: Substitution-structured B5 arithmeticReplace the cannibalization debit:expectedSingleUnits = (bucketMeanNet + singleProductionCost) ÷ exported singleNetMarginPerUnit (default $0.40, documented as the implied per-unit label net; derivation from price/pressing/skim/royalty conventions shown in audit).substitutionPropensity = clamp(substitutionK × albumDemandFactor(genre, year), 0, substitutionCap) — exported substitutionK default 1.0, substitutionCap default 0.85. The album demand factor is the same term already computed in the album prior; one source, two readers.divertedUnits = substitutionPropensity × expectedOverlapFraction × expectedSingleUnits (overlap stays (gap-adjusted) 0.60 as built).cannibalizationLoss = divertedUnits × albumMarginPerUnit (the existing at-margin value). cannibalizationStrength no longer appears in the decision estimate.Promo branch: promoLift + expectedSingleNet − cannibalizationLoss, unchanged otherwise. Deterministic, no RNG, evaluated after the frozen format decision as before.Part 3: Applied-side consistency (B4)suppression = cannibalizationStrength × singleHeat × substitutionPropensity(genre, year) — same propensity function, same clamp. Unlinked/retired-link inertness rules unchanged (propensity multiplies an already-zero term). Report the new demand-weighted suppression for 1960 (expected: well under 1%).Part 4: Validation (52 weeks, seeds 1001/1002/1003 + disabled baseline)All current gates at the repaired Pearson bands; checksums exact; determinism 2× seed-1001.Closed-form reachability re-run, both vectors, term-by-term (audit requirement): (a) 1960 adult New/Unsigned selects promo; (b) 1968-curve Q4 Superstar selects standalone. If (b) fails to flip under the new structure, stop and report — no constant chasing.1960 dynamic standalone share: report; ≈0% remains the expected and correct result.New sanity check: report the B5 promo-vs-standalone margin distribution by genre group — the margin should narrow as album demand factor rises even within 1960 (adult acts closer to indifference than youth). That gradient is the observable precursor of the late-decade flip and our best 52-week evidence the seam works.Watch cohorts continue unchanged.GuardrailsFrozen: Single table, album scalars, comp weights, freshness factor/counters, all demand/chart/retirement/generation constants outside the specified B4 line.No stature or year conditionals anywhere in the new arithmetic — propensity must come exclusively through the affinity input.If seed-level youth or adult mix drifts out of band from the B4 suppression change (unlikely — it weakens an already-small 1960 effect), stop and report rather than retune scalars.
